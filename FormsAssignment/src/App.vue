<template>
    <div class="container">
        <form>
          <h1>Tess' Form of Wonder! </h1>
            <div class="row">
                <div class="col-xs-12 col-sm-8 col-sm-offset-2 col-md-6 col-md-offset-3">
                  <app-full-name v-model="fullName"></app-full-name>
                    <div class="form-group">
                        <label>Email Address</label>
                        <input
                                type="email"
                                class="form-control"
                                v-model.lazy="email">
                    </div>
                    <div class="form-group">
                        <label>Password</label>
                        <input
                                type="password"
                                class="form-control"
                                v-model.lazy="password">
                    </div>
                    Store Data?
                    <div class="form-group">
                        <label><input type="radio" value="yes" v-model="storeData">Yes
                        </label>
                        <label><input type="radio" value="no" v-model="storeData">No
                        </label>
                    </div>
                    <button type="submit" @click.prevent="showForm()" class="btn btn-primary">Submit Form</button>

                    <!-- Exercise 2 -->
                    <!-- Only display the Form if it has NOT been submitted -->
                    <!-- Display the Data Summary ONCE the Form HAS been submitted -->

                    <!-- Exercise 3 -->
                    <!-- Edit the Example from above and create a custom "Full Name" Control -->
                    <!-- which still holds the First Name and Last Name Input Field -->
                </div>
            </div>
        </form>
        <hr>
        <div class="row" v-if="isSubmitted">
            <div class="col-xs-12 col-sm-8 col-sm-offset-2 col-md-6 col-md-offset-3">
                <div class="panel panel-default">
                    <div class="panel-heading">
                        <h4>Your Data</h4>
                    </div>
                    <div class="panel-body">
                        <p>Full Name: {{fullName}}</p>
                        <p>Mail: {{email}}</p>
                        <p>Password: {{password}}</p>
                        <p>Store in Database?: {{storeData}}</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</template>

<script>
import Name from './Components/Name.vue';
export default {
      data() {
        return {
            fullName: 'Tess Elliott',
            password: '',
            email: '',
            storeData: 'yes',
            isSubmitted: false,
        }
      },
      methods: {
        showForm() {
          this.isSubmitted = true;
        }
      },
      components: {
        appFullName: Name,
      }
    }
</script>

<style>
</style>
